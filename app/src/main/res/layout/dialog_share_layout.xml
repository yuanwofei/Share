<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="fill_parent"
    android:layout_height="fill_parent"
    android:background="@drawable/dialog_share_layout_shape"
    android:orientation="vertical" >

    <TextView
        android:id="@+id/dialog_share_title"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="5dp"
        android:layout_marginBottom="15dp"
        android:gravity="center"
        android:text="分享到"
        android:textColor="@android:color/black"
        android:textSize="17sp" />

    <GridView
        android:id="@+id/dialog_share_gridview"
        android:layout_width="fill_parent" 
        android:layout_height="0dp"
        android:layout_weight="1.0"
        android:layout_marginLeft="10dip"
    	android:layout_marginRight="10dip"
        android:cacheColorHint="#00000000"
        android:fadingEdge="none"
        android:listSelector="#00000000"
        android:numColumns="3"
        android:scrollbarStyle="outsideOverlay"
        android:verticalSpacing="20dip" />

    <Button
        android:id="@+id/dialog_share_close_btn"
        android:layout_width="fill_parent"
        android:layout_height="40dp"
        android:layout_marginTop="15dp"
        android:layout_marginBottom="5dp"
        android:layout_marginLeft="15dip"
    	android:layout_marginRight="15dip"
    	android:background="@drawable/dialog_share_close_btn_selector"
        android:text="取消"
        android:textColor="@android:color/black"
        android:textSize="17sp" />

</LinearLayout>